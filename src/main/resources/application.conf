ktor {
  deployment {
    port = 8080
  }
  application {
    modules = [no.nav.tsm.ApplicationKt.module]
  }
}

postgres {
  url = ${DB_JDBC_URL}
  username = ${DB_USERNAME}
  password = ${DB_PASSWORD}
}
