package no.nav.tsm.core.common

/**
 * This represents the DiagnoseSystem that syk-inn(-api) supports, both as payload values and
 * internally in domain objects.
 */
enum class SykInnDiagnoseSystem {
    ICPC2,
    ICD10,
    ICPC2B,
}
